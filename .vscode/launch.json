{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension",
            "type": "extensionHost",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}/tests/application"
            ],
            "outFiles": [
                "${workspaceFolder}/output/dist/**/*.js"
            ],
            "preLaunchTask": "${defaultBuildTask}",
            "env": {
                "NODE_NO_WARNINGS": "1"
            }
        }
    ]
}