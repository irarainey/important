# Intentionally empty — exists only so that "requests" appears
# as a deeply nested directory name in the workspace filesystem.
# This should NOT cause `import requests` to be categorised as "local".
